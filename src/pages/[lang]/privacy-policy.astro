---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { SUPPORTED_LOCALES, getTranslations, type SupportedLocale } from '../../i18n/utils';
import { Shield } from 'lucide-react';

export function getStaticPaths() {
  return SUPPORTED_LOCALES.filter((l) => l !== 'en').map((lang) => ({
    params: { lang }
  }));
}

const { lang } = Astro.params;
const activeLang = (lang || 'es') as SupportedLocale;
const t = getTranslations(activeLang);

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": `${t.privacy.title} - Build Yardage`,
  "url": `https://buildyardage.com/${activeLang}/privacy-policy`
};
---

<BaseLayout
  title={`${t.privacy.title} | Build Yardage`}
  description={t.privacy.intro}
  jsonLd={jsonLd}
  lang={activeLang}
>
  <section class="bg-canvas border-b border-hairline py-12 sm:py-16 transition-colors duration-200">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <nav class="flex text-xs text-muted mb-4 gap-2">
        <a href="/" class="hover:text-ink">{t.common.home}</a>
        <span>/</span>
        <span class="text-brand-accent font-semibold">{t.privacy.title}</span>
      </nav>
      <h1 class="text-3xl sm:text-5xl font-extrabold text-ink tracking-tight mb-3 max-w-3xl">
        {t.privacy.title}
      </h1>
      <p class="text-xs font-mono text-muted">{t.privacy.lastUpdated}</p>
    </div>
  </section>

  <section class="mx-auto max-w-4xl px-4 py-16 sm:px-6 lg:px-8 space-y-8">
    <p class="text-sm text-body leading-relaxed">{t.privacy.intro}</p>

    <div class="space-y-4">
      <h2 class="text-lg font-bold text-ink">{t.privacy.noTrackingTitle}</h2>
      <p class="text-xs text-muted leading-relaxed">{t.privacy.noTrackingDesc}</p>
    </div>

    <div class="space-y-4">
      <h2 class="text-lg font-bold text-ink">{t.privacy.localStateTitle}</h2>
      <p class="text-xs text-muted leading-relaxed">{t.privacy.localStateDesc}</p>
    </div>

    <div class="space-y-4">
      <h2 class="text-lg font-bold text-ink">{t.privacy.cookiesTitle}</h2>
      <p class="text-xs text-muted leading-relaxed">{t.privacy.cookiesDesc}</p>
    </div>
  </section>
</BaseLayout>
