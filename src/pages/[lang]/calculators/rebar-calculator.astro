---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import RebarCalculator from '../../../components/RebarCalculator';
import ToolGuideSection from '../../../components/ToolGuideSection.astro';
import { SUPPORTED_LOCALES, getTranslations, LOCALES, type SupportedLocale } from '../../../i18n/utils';

export function getStaticPaths() {
  return SUPPORTED_LOCALES.filter((l) => l !== 'en').map((lang) => ({
    params: { lang }
  }));
}

const { lang } = Astro.params;
const activeLang = (lang || 'es') as SupportedLocale;
const t = getTranslations(activeLang);

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": `${t.nav.rebar} - Build Yardage`,
  "url": `https://buildyardage.com/${activeLang}/calculators/rebar-calculator`,
  "applicationCategory": "UtilityApplication",
  "operatingSystem": "All"
};
---

<BaseLayout
  title={`${t.nav.rebar} | Build Yardage`}
  description="High-precision steel rebar grid reinforcement and 40d lap splice calculator."
  jsonLd={jsonLd}
  lang={activeLang}
>
  <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
    <RebarCalculator client:load initialIsMetric={LOCALES[activeLang].isMetricDefault} />
    <ToolGuideSection slug="rebar-calculator" />
  </div>
</BaseLayout>
